# Local Terraform directories
.terraform/
.terraform.lock.hcl

# .tfstate files
*.tfstate
*.tfstate.backup

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data,
# except for the example file.
*.tfvars
!*.tfvars.example

# Ignore override files as they are usually temporary
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI configuration files
.terraformrc
terraform.rc

# Python cache files + Large files
lambda_package/
genmab_genai_env/
results/
Reviews.csv
model.safetensors
model.tar.gz
deployment_package.zip